{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -f \"dist/src/main.js\" ]; then npx prisma migrate deploy && npx prisma db seed && node dist/src/main.js; else cd backend && npx prisma migrate deploy && npx prisma db seed && node dist/src/main.js; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
